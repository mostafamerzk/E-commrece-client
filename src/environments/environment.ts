export const environment = {
  production: false,
  // During development, the app talks to your local backend.
  // Change this to your teammate's machine IP if you want to share a backend.
  apiUrl: 'http://localhost:3000/api',
};
